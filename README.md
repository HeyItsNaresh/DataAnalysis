How to run the Toolkit

PRE-REQUISITE : Python should be installed on your system

1- Unzip the ZN-Analytics-Toolkit file
2- Open the terminal INSIDE the unzipped folder
3- Run the command "pip install -r requirements.txt"
4- Once all the downloads are finished, run the command "python -m streamlit run app.py"
5- Explore the toolkit, enjoy
